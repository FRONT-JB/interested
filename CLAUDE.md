# interested

## Agent skills

### Issue tracker

이슈는 GitHub Issues(`FRONT-JB/interested`)에 `gh` CLI로 관리합니다. See `docs/agents/issue-tracker.md`.

### Triage labels

다섯 개 표준 역할을 이모지 + 한글 라벨로 매핑해 씁니다. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — 루트 `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.
