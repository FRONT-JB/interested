---
facts: df3468892c86
prose: template
---

# Portrait

`forward-deployed-engineering`, `shared-primitives`, `single-source-of-truth`에 관심이 있다.

연속으로 나타난 주 — `forward-deployed-engineering` 1주, `shared-primitives` 1주, `single-source-of-truth` 1주

가장 최근에 Note를 쓴 주는 2026-W34이다. Note는 지금까지 4편이고 3주에 걸쳐 썼다. 마지막 Note는 2026-08-17에 썼다.

## 관심

- `agent-harness` 1편
- `deferred-processing` 1편
- `forward-deployed-engineering` 1편
- `harness-evaluation` 1편
- `in-distribution` 1편
- `llm-knowledge-base` 1편
- `memory-tiering` 1편
- `parameter-object-pattern` 1편
- 그 밖에 4개

## 행동

2026-08-11부터 2026-08-17까지 공개 푸시 24회.

- `FRONT-JB/interested` 12회 — 주 언어 `TypeScript`
- `FRONT-JB/mcx` 9회 — 주 언어 `Python`
- `claude-studio/built` 3회 — 주 언어 `JavaScript`

## 대비

- 관심이 몰린 곳은 `forward-deployed-engineering`, `shared-primitives`, `single-source-of-truth`, 푸시가 몰린 곳은 `FRONT-JB/interested` 12회. 그 저장소의 주 언어는 `TypeScript`.
