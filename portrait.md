# Portrait

`parameter-object-pattern`, `query-object`, `stamp-coupling`에 관심이 있다.

연속으로 나타난 주 — `parameter-object-pattern` 1주, `query-object` 1주, `stamp-coupling` 1주

가장 최근에 Note를 쓴 주는 2026-W28이다. Note는 지금까지 1편이고 1주에 걸쳐 썼다. 마지막 Note는 2026-07-08에 썼다.

## 관심

- `parameter-object-pattern` 1편
- `query-object` 1편
- `stamp-coupling` 1편

## 행동

2026-08-08부터 2026-08-14까지 공개 푸시 70회.

- `FRONT-JB/mcx` 55회 — 주 언어 `Python`
- `claude-studio/built` 6회 — 주 언어 `JavaScript`
- `claude-studio/geness` 3회
- `FRONT-JB/interested` 3회
- `FRONT-JB/skills` 3회

## 대비

- 관심이 몰린 곳은 `parameter-object-pattern`, `query-object`, `stamp-coupling`, 푸시가 몰린 곳은 `FRONT-JB/mcx` 55회. 그 저장소의 주 언어는 `Python`.
