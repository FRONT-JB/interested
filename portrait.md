---
facts: eae0f4836765
prose: model
---

# Portrait

`forward-deployed-engineering`, `shared-primitives`, `single-source-of-truth`에 관심이 있다. 2026-W34 한 주에 이 셋이 함께 몰렸고, 각각은 앞뒤로 이어지지 않은 채 1주씩만 나타났다. 지금까지 쓴 Note는 4편이고 3주에 걸쳐 있으며, 가장 최근 Note는 2026-08-17에 썼다.

누적을 보면 이 세 이름을 포함해 `agent-harness`, `deferred-processing`, `harness-evaluation`, `in-distribution`, `llm-knowledge-base`, `memory-tiering`, `parameter-object-pattern`, `query-object`, `stamp-coupling`까지 열두 개의 이름이 각각 1편씩 흩어져 있다. 그와 다른 기간인 2026-08-11부터 2026-08-17까지 공개 푸시 23회 중 `FRONT-JB/interested`(주 언어 `TypeScript`)로 11회, `FRONT-JB/mcx`(주 언어 `Python`)로 9회, `claude-studio/built`(주 언어 `JavaScript`)로 3회가 갔다. Note가 가리키는 이름들과 푸시가 몰린 `FRONT-JB/interested`는 겹치지 않는다.
